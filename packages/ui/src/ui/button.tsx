import React from "react";

const Button = () => {
  return (
    <button className=" bg-blue-500 hover:bg-blue-700 transition-colors font-bold p-2 rounded ">
      Button
    </button>
  );
};

export default Button;
